RSpec.describe VanderveurViewTool do
  it "has a version number" do
    expect(VanderveurViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
